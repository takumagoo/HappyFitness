<!--ログイン画面-->

<%= render 'public/header' %>

<div class="container">
  <h5 class="bg-secondaryk">ログイン</h5>

  <div class="row">
    <div class="col-8">
      <p>会員の方はこちらからログイン</p>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 d-flex justify-content-center ">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="row">
            <div class="col-5 pb-3"><%= f.label :email, 'メールアドレス'  %></div>
            <div class="col-7 pb-3"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
        </div>
        <div class="row">
          <div class="col-5 pb-3"><%= f.label :password, 'パスワード' %></div>
          <div class="col-7 pb-3"><%= f.password_field :password, autocomplete: "current-password" %></div>
        </div>
        <div class="row">
          <div class="col-5"></div>
          <div class="col-7"><%= f.submit "ログイン", class: "btn btn-primary" %></div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-8">
      <p>会員登録がお済でない方</p>
      <%= link_to 'こちら', new_customer_registration_path %>から新規登録してください。
    </div>
  </div>
</div>

<%= render 'public/footer' %>
